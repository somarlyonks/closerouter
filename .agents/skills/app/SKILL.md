---
name: app
description: Guide for developing the CloseRouter macOS app (SwiftUI/Xcode) in app/ — build process, architecture, file map, server API contract, conventions, and known pitfalls. Use whenever working on the macOS app, adding features, or touching app/ code.
disable-model-invocation: true
---

# CloseRouter macOS App

A native **SwiftUI menu-bar + window app** that is the **primary way to run CloseRouter**. It bundles
the compiled `dist/closerouter` binary as a resource, spawns it as a managed child process, and talks
to it over the existing local HTTP API. The web UI stays as-is (still reachable at `localhost:6712`).

The authoritative plan (vision, confirmed decisions, milestones) lives in **`plan.md`**. Progress is
tracked in **`closerouter.todo`** (see the `todo` skill). This skill is the implementation reference.

## Build & run

- **Release app (canonical):** `npm run build` — runs `build:cli` (compiles the closerouter binary to
  `dist/closerouter`) then `xcodebuild -scheme CloseRouter -configuration Release`. Output lands in
  `~/Library/Developer/Xcode/DerivedData/CloseRouter-*/Build/Products/Release/CloseRouter.app`.
- **Binary only:** `npm run build:cli` (`scriptc build lib/cli.ts --ffi native/ffi.json -o dist/closerouter`).
- **Debug (fast):** `xcodebuild -project app/CloseRouter.xcodeproj -scheme CloseRouter -configuration Debug -derivedDataPath app/build/DerivedData build` then run
  `app/build/DerivedData/Build/Products/Debug/CloseRouter.app`.
- **In Xcode:** open `app/CloseRouter.xcodeproj`, hit Run. A "Embed closerouter binary" Run Script phase
  builds `dist/closerouter` if missing and copies it into `Contents/Resources/closerouter` (ad-hoc signed).
- `app/build/` is gitignored. The app icon is generated by `app/scripts/make-icon.swift`
  (`swift scripts/make-icon.swift <appiconset-dir>`), not committed as hand-edited PNGs.

## Architecture

Managed-child-process model (no entitlements, no system extensions needed):

```
CloseRouter.app
  └── Contents/Resources/closerouter   ← spawned with: server -c <configPath>
        └── local HTTP server on 127.0.0.1:<port> (default 6712)
```

- The app **owns the config file**: `~/Library/Application Support/CloseRouter/closerouter.json`
  (`ConfigStore`), so it reads the `key` from disk to authenticate — no prompts.
- Config storage is strict JSON (server's `parseConfig` uses `JSON.parse`). No JSON5.
- Menu bar is the **dominant surface** (SFM-style): `.accessory` activation + window closed when
  launched as a login item; the status-bar menu has Start/Stop, status, Open, Quit.

### Server API the app consumes

| Endpoint | Auth | Used for |
|---|---|---|
| `GET /status` | none | health check → `{version, status}` |
| `GET /config` | `Bearer <key>` | overview providers; requires `Accept: application/json` |
| `PUT /config` | `Bearer <key>` | save config (validates server-side) |
| `GET /usage` | `Bearer <key>` | `{count, inTokens, outTokens, cachedTokens}` |
| `GET /v1/models` | `Bearer <key>` | `{object, data:[{id:"provider/model",owned_by}]}` |
| `GET /logs` SSE | `Cookie: cr-key=<key>` | live log events + 3s `event: ping` |
| `GET /logs` JSON | `Cookie: cr-key=<key>` | history → `{entries:[...]}` |

## File map

```
app/CloseRouter/
  CloseRouterApp.swift        @main App, WindowGroup "main"
  AppDelegate.swift           lifecycle, login-item detection, server auto-start on launch
  StatusBarController.swift   NSStatusItem menu (status, Start/Stop, Open, Quit)
  Core/
    ServerManager.swift       spawn/monitor/restart child, /status health, uptime, notifications
    ConfigStore.swift         config path, defaults, read/save/port helpers
    APIClient.swift           HTTP client (Bearer + cookie auth) + Overview DTOs + ConfigValidator
    LogStream.swift           LogEntry/LogHistoryEntry models + LogsViewModel (SSE, pause/filter)
    Preferences.swift         UserDefaults-backed prefs (launchAtLogin, startServerOnLaunch, ...)
    AppNotifications.swift    UNUserNotificationCenter wrapper
  Editor/
    CodeTextView.swift        JSON syntax-highlight NSTextStorage + NSTextView (⌃Space/F5 complete)
    SchemaCompletions.swift   JSONSchema model + ConfigCompletionEngine (schema-driven completion)
    ConfigEditorView.swift    the Config page: toolbar (Validate/Format/Save ⌘S) + status bar
  Views/
    MainView.swift            NavigationSplitView shell + AppSection enum + AppState (section switch)
    SidebarView.swift         sidebar list
    OverviewView.swift        dashboard (status banner, stat cards, providers, models)
    LogsView.swift            live SSE table + detail inspector
    SettingsView.swift        launch-at-login (SMAppService), prefs, server controls, about
```

## Conventions

- **All Core singletons are `@MainActor`** and accessed as `X.shared` (`ServerManager.shared`,
  `StatusBarController.shared`). View models are `@MainActor final class … ObservableObject`.
- **UI → data** goes through `ServerManager` (state) + `APIClient`; views never spawn processes.
- **Auth**: `Bearer` for `/config`, `/usage`, `/v1/models`; `Cookie: cr-key=` for `/logs` (SSE + JSON).
  Read the key from `ConfigStore.read().key`, never prompt.
- **JSONSchema is a `final class`** (not a struct) — it recursively contains itself.
- **Deployment target is 14.0** (`MACOSX_DEPLOYMENT_TARGET` in both project configs).
- **`project.pbxproj` is hand-written** (no xcodegen). Any new file must be registered manually in 4
  places: a PBXFileReference, a PBXBuildFile, the owning PBXGroup `children`, and the Sources (or
  Resources) phase. Keep IDs as unique 24-char hex strings (the file uses `0000000000000000000000XX`).
- **The schema** is referenced in-place as `../../closerouter-schema.json` (relative to the
  `CloseRouter` group) and copied to Resources, so it stays in sync with the repo root automatically.
- When adding a sidebar page: add a case to `AppSection` (defined in `MainView.swift`), give it
  `title` + `systemImage`, and add a `case` to the detail switch.
- Track every change in `closerouter.todo` (see the `todo` skill).

## Interactive GUI debugging (Accessibility API + CGEvent)

For UI bugs you can't reproduce headlessly ("can't click / can't type / layout broken"), drive the
running app over the Accessibility API from a throwaway `swift` script — no Xcode, no UI test target.
Used this way to find and fix the config editor being unclickable (zero-height text view, below).

**Prereq:** `AXIsProcessTrusted()` must be true (the terminal/shell running the script needs
Accessibility permission). If `osascript` System Events says "not allowed assistive access", the
AX API from a compiled `swift` script can still work — check it, don't trust osascript's verdict.

Key facts learned the hard way:

- **AX coordinates are TOP-LEFT origin in global screen space.** Feed `kAXPositionAttribute`
  straight into CGEvent clicks. Do NOT flip `y` (screenH - y) — a flipped click will hit the wrong
  part of the window (it once hit the close button and closed the window).
- **`AXPress` on a SwiftUI sidebar row "succeeds" but doesn't select.** For `NavigationSplitView`
  rows, post a real mouse down/up at the row's center instead (`AXUIElementPerformAction` returns
  success but the selection never changes).
- **CGEvent clicks land on whatever window is under the point**, so the target app must be
  frontmost first (`osascript -e 'tell application "CloseRouter" to activate'`).
- Dump the AX tree to find elements: `AXTextArea` (an NSTextView), `AXScrollArea`, `AXRow` with
  `AXStaticText` children holding the label, `AXButton` whose title lives in `kAXDescriptionAttribute`.
  A `walk` helper recursing `kAXChildrenAttribute` is all you need.
- After a click, confirm focus took with `kAXFocusedAttribute` on the text area before assuming
  the click registered; read/write content via `kAXValueAttribute`.
- **Always check element sizes.** A `AXScrollArea` with normal size but a `AXTextArea`/documentView
  reporting zero height is the classic SwiftUI + NSScrollView bug below.

Minimal pattern: `walk` the tree to the element, read `kAXPositionAttribute`/`kAXSizeAttribute`
(convert with `AXValueGetValue`), click at `pos + size/2`, then re-read `kAXValueAttribute` and
compare lengths to prove the edit stuck. Restart the app fresh before asserting on status text
(e.g. "No unsaved changes" on a pristine config) so prior test typing can't leak in.

**Headless fallback:** if you just need frame geometry or `insertText` behavior, instantiate the
same AppKit objects (`NSTextStorage`/`NSLayoutManager`/`NSTextContainer`/`NSTextView`/`NSScrollView`)
in a plain `swift script` inside an offscreen `NSWindow` and force layout
(`layoutManager?.ensureLayout(for:)`). This reproduced the zero-height bug without any GUI access.

## Known pitfalls (learned the hard way)

- **NSTextView in a programmatic `NSScrollView` lays out at ZERO height** unless you set
  `isVerticallyResizable = true`, `minSize`/`maxSize`, and `autoresizingMask = [.width]`. Symptom:
  the scroll view is correctly sized but its documentView is invisible and unclickable — clicks
  never focus it and typing does nothing. The AX tree reports the `AXScrollArea` at full size but
  its `AXTextArea` at `height: 0`. This is what made the config editor uneditable.
- **Dirty status flips on load.** `.onChange(of: text)` fires *after* `onAppear`'s `loadConfig()`
  resets `isDirty`, so a pristine config showed "Unsaved changes" with an enabled Save. Guard the
  load-time swap with a `suppressNextChange` flag instead of tracking dirty purely off `text`.
- **`/logs` JSON history ≠ live SSE shape.** History entries come from the usage DB: numeric `id`,
  the request UUID in `requestId`, and **no `phase`** field. Decode with `LogHistoryEntry` (map to
  `LogEntry` via `requestId`), not `LogEntry`.
- **Token usage covers non-streaming responses.** Streaming usage is extracted live by the proxy
  (`feedStreamUsage` parses `data:` frames into `responseLog.usage`); non-streaming leaves `usage` as
  an empty `{}`, and `logMiddleware` (`lib/server/logs/index.ts`) falls back to
  `extractTokenUsage(responseLog?.body)` — which only worked once `hasUsage` stopped treating the
  empty `{}` as authoritative. If token counts look missing again, check that fallback path.
- **`/config` needs `Accept: application/json`** or it returns the HTML page.
- **Model addressing is `provider/model`** (e.g. `mock/mock-1`), not bare model ids.
- **Crash-restart guard**: `start()` rejects when `state.isTransitioning`; an unexpected-exit restart
  must set `state = .stopped` before the backoff delay, or the restart silently no-ops.
- **NSTextView completion**: the partial word range excludes the opening `"`, so `insertCompletion`
  extends the replacement range to include it, and quoted inserts land cleanly. Trigger is ⌃Space/F5
  (`CompletingTextView.performKeyEquivalent`).
- **SwiftUI gotchas**: struct views can't `[weak self]` in Combine sinks (capture directly — `@State`
  is reference-backed); `Text(x, format: .dateTime…second(.standard))` doesn't compile — use
  `x.formatted(date: .omitted, time: .standard)`.
- **Restart on port change**: after saving a config with a new `port`, call `ServerManager.restart()`
  (the server only logs "restart for the new port to take effect" otherwise).
- **Login item**: `SMAppService.mainApp.register()` may need the app in `/Applications` to persist;
  check `SMAppService.mainApp.status` and surface `.requiresApproval`. Launch-as-login-item detection
  uses `kAELaunchedAsLogInItem` (see `AppDelegate`).
- The old placeholder `Views/ConfigView.swift` was deleted; the editor is `Editor/ConfigEditorView.swift`.
- **The main window is AppKit-owned, NOT a WindowGroup** (`MainWindowController` in
  `CloseRouterApp.swift`). Closing destroys WindowGroup windows, which made "Open CloseRouter"
  unrecoverable: captured `openWindow` actions go stale once their window unmounts, and SwiftUI
  clears/resets any `NSWindowDelegate` you install (so close-to-hide interception silently stops
  working). The single `NSWindow` hosts `MainView` via `NSHostingView`, is `isReleasedWhenClosed =
  false`, and its delegate turns close into `orderOut`. Reopening is just `makeKeyAndOrderFront`.
- **`NSApp.delegate` is `SwiftUI.AppDelegate`, not your `@NSApplicationDelegateAdaptor` class.**
  `(NSApp.delegate as? AppDelegate)` always fails — use the `AppDelegate.shared` static set in
  `applicationDidFinishLaunching`.
