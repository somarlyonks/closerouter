CloseRouter:
    core:
        [x] config loading & validation
    cli:
        [x] help
        server:
            [x] detach
            [x] config path
        [x] version
    server:
        [x] router
        [x] auth (bearer key, cookie)
        [x] proxy forwarding with SSE streaming
        routes:
            v1:
                [x] chat/completions
                [x] models
                [x] responses
            [x] config
            [x] logs
            [x] status
            [x] usage
    db:
        [x] SQLite ABI
        [x] sql interface functions
        [x] tests
        [x] config field
        [x] in momery
        [x] persistent
    app:
        scaffold:
            [x] xcode project
            [x] bundle closerouter binary
            [x] menu bar icon + empty window
            [x] run script build wiring
        process core:
            [x] ServerManager (spawn/monitor/restart)
            [x] health check via /status
            [x] menu bar start/stop + open window
            [x] default config in Application Support
        config editor:
            [x] CodeTextView (NSTextView)
            [x] schema completions
            [x] validation + format
            [x] save via PUT /config
        logs:
            [x] SSE stream view
            [x] pause/filter/clear
            [x] history on open
        polish:
            [x] launch at login (SMAppService)
            [x] notifications on crash/start
            [x] settings
        overview:
            [ ] status banner
            [ ] usage totals
            [ ] provider/model list
