CloseRouter:
    core:
        [x] config loading & validation
    cli:
        [x] help
        server:
            [x] detach
            [x] config path
        [x] version
    server:
        [x] router
        [x] auth (bearer key, cookie)
        [x] proxy forwarding with SSE streaming
        routes:
            v1:
                [x] chat/completions
                [x] models
                [x] responses
            [x] config
            [x] logs
            [x] status
            [x] usage
    db:
        [x] SQLite ABI
        [x] sql interface functions
        [x] tests
        [x] config field
        [x] in momery
        [x] persistent
    app:
        scaffold:
            [x] xcode project
            [x] bundle closerouter binary
            [x] menu bar icon + empty window
            [x] run script build wiring
        process core:
            [ ] ServerManager (spawn/monitor/restart)
            [ ] health check via /status
            [ ] menu bar start/stop + open window
            [ ] default config in Application Support
        config editor:
            [ ] CodeTextView (NSTextView)
            [ ] schema completions
            [ ] validation + format
            [ ] save via PUT /config
        logs:
            [ ] SSE stream view
            [ ] pause/filter/clear
            [ ] history on open
        polish:
            [ ] launch at login (SMAppService)
            [ ] notifications on crash/start
            [ ] settings
        overview:
            [ ] status banner
            [ ] usage totals
            [ ] provider/model list
